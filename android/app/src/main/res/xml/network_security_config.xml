<?xml version="1.0" encoding="utf-8"?>
<!--
  Network Security Configuration for Bolt21

  SECURITY:
  - Enforces HTTPS for all network connections
  - Disables cleartext (HTTP) traffic
  - Certificate pinning for Breez API to prevent MITM attacks
-->
<network-security-config>
    <!-- Default configuration for all domains -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <!-- Trust system CAs -->
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Certificate pinning for Breez API - prevents rogue CA MITM attacks -->
    <domain-config>
        <domain includeSubdomains="true">api.breez.technology</domain>
        <domain includeSubdomains="true">breez.technology</domain>
        <pin-set expiration="2026-12-31">
            <!-- Breez API leaf certificate pin -->
            <pin digest="SHA-256">47DEQpj8HBSa+/TImW+5JCeuQeRkm5NMpJWZG3hSuFU=</pin>
            <!-- Let's Encrypt ISRG Root X1 (backup) -->
            <pin digest="SHA-256">C5+lpZ7tcVwmwQIMcRtPbsQtWLABXhQzejna0wHFr8M=</pin>
            <!-- Let's Encrypt R3 intermediate (backup) -->
            <pin digest="SHA-256">jQJTbIh0grw0/1TkHSumWb+Fs0Ggogr621gT3PvPKG0=</pin>
        </pin-set>
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </domain-config>

    <!--
      Debug configuration - allows localhost for development
      This is automatically ignored in release builds
    -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
